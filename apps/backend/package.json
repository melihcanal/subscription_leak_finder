{
  "name": "@slf/backend",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local",
    "build": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@slf/shared": "workspace:*",
    "@slf/db": "workspace:*",
    "drizzle-orm": "^0.33.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260317.1",
    "typescript": "^5.6.2",
    "wrangler": "^4.0.0"
  }
}
